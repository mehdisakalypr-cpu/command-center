// Cockpit centralisé multi-projets — réutilise l'UI riche d'/admin/simulator
// (PRODUCT_DEFAULTS = ofa/ftg/estate/shiftdynamics/cc, funnel + matrix + push agents).
// Règle CC: V/R+campagnes+revenus dans /dashboard, pas dans /admin (cf feedback_cc_centralise).
export { default } from '../../admin/simulator/page'
